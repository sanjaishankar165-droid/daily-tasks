#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i=0;
    scanf("%d", &n);
    while(n>9){
        i+=n%10;
        n=n/10;
    }
    i=i+n;
   printf("%d",i);
       return 0;
}
